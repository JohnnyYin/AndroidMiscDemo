<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/change_theme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/change" />

    <TextView
        android:id="@+id/switch_page"
        style="@style/BaseClickable"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_below="@+id/change_theme"
        android:gravity="center_vertical"
        android:padding="5dp"
        android:text="switch_page" />

    <com.johnnyyin.themedemo.MyListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/switch_page"
        android:listSelector="@drawable/item_background" />

</RelativeLayout>
